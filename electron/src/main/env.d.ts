/** Injected by electron.vite.config.ts (main + preload) from frontend/package.json. */
declare const __APP_VERSION__: string;
/** Injected at build time so fork editions cannot inherit upstream update feeds. */
declare const __VOICESTUDIO_EDITION__: 'standard' | 'gemini';
